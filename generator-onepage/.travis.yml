language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-onepage' ]; then cd .. && eval "mv $currentfolder generator-onepage" && cd generator-onepage; fi

